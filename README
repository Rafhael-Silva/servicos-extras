# Servicos Extras

Repositório da plataforma Serviços Extras.
A solução é composta por microserviços independentes, responsáveis por autenticação, gerenciamento de usuários, vagas, processos seletivos, avaliações, pagamentos e roteamento das requisições por meio de um API Gateway.

## Sobre o projeto

Serviços Extras é uma plataforma que conecta candidatos e recrutadores para oportunidades de serviços extras. A plataforma permite o cadastro de usuários, gerenciamento de vagas, processos seletivos, avaliações, pagamentos e autenticação segura, utilizando uma arquitetura baseada em microserviços.

## Microserviços

### auth-service (Concluído)

- Autenticação, registro, login, controle de tokens, autorização básica.
- Não armazena dados detalhados dos usuários.

#### Funcionalidades implementadas:

- Registro de usuários
- Verificação de e-mail
- Login com autenticação em duas etapas
- Autenticação com JWT (Access Token e Refresh Token)
- Logout
- Recuperação de senha
- Alteração de senha
- Consulta de perfil
- Testes unitários
- Testes de integração

#### Cobertura de testes:

- Statements: 98.67%
- Branches: 96.13%
- Functions: 89.77%
- Lines: 98.67%

### user-service (Concluído)

- Gerencia dados e perfis dos usuários (candidatos e recrutadores).
- Armazena informações pessoais e perfil do usuário.

#### Funcionalidades implementadas:

- Criação e atualização de perfis
- Consulta de perfil
- Criação e atualização de currículos pela plataforma
- Upload de arquivos (Foto/Logo/Currículo)
- Testes unitários
- Testes de integração

#### Cobertura de testes:

- Statements: 100%
- Branches: 99.08%
- Functions: 100%
- Lines: 100%

### job-service (Não iniciado)

- Criação e gerenciamento de vagas ou trabalhos disponíveis.
- Não lida diretamente com dados pessoais.

### selection-service (Não iniciado)

- Gerencia processos seletivos, triagem, status da seleção e integração com avaliações técnicas.
- Não armazena dados cadastrais.

### evaluation-service (Não iniciado)

- Avaliações técnicas, feedbacks.
- Focado exclusivamente em avaliações.

### payment-service (Não iniciado)

- Processa pagamentos e transações financeiras.
- Não armazena dados pessoais.

### gateway (Iniciado)

- API Gateway responsável por rotear requisições para os microserviços.
- Controla autenticação básica, rate limiting e logs.
- Não contém lógica de negócio.

---

## Arquitetura

- Arquitetura baseada em microserviços.
- APIs REST.
- Comunicação entre serviços via HTTP.
- Banco de dados independente por microserviço.

## Tratamento de erros

- Tratamento centralizado de erros por meio do errorHandler.
- Gerenciamento de erros em operações assíncronas por meio do asyncHandler.

## Tecnologias utilizadas

### Linguagem e framework

- Node.js
- Express

### Banco de dados

- PostgreSQL
- Prisma ORM

### Autenticação e segurança

- JWT (Access Token e Refresh Token)
- HTTP-only cookies
- Bcrypt (hash de senhas)
- Node.js Crypto (hash de tokens e códigos de verificação)
- Helmet (segurança de headers HTTP)
- Express-rate-limit (proteção contra brute force e DDOS)
- CORS (controle de acesso entre domínios)

### Validação de dados

- Joi

### HTTP e integração entre serviços

- Axios (requisições HTTP entre microserviços)
- http-proxy-middleware (proxy de requisições, útil no gateway)

### Serviços externos

- Serviços SMTP (envio de emails)
- Nodemailer (integração com SMTP para envio de emails)

### Upload de arquivos

- Multer
- AWS SDK v3 (Amazon S3)
- LocalStack (ambiente local para S3)

### Logs e monitoramento

- Winston (Logger personalizado)

### Automação e tarefas agendadas

- Node-cron (execução de tarefas agendadas)

### Testes e desenvolvimento

- Jest (testes unitários e de integração)
- Supertest (testes de rotas HTTP)
- Nodemon (desenvolvimento com auto-reload)

---
